/node_modules
/.env

/newrelic_agent.log

# Ignore package-lock.json, I prefer yarn.lock
package-lock.json

# Should ignore code coverage reports

/coverage
/.nyc_output

# There shouldn't every be an SQL-file committed, but for debugging purposes they may be handy

*.sql

# Left for historic reason, was used by an old removed migration system
# Will eventually also be removed

/database.json
